/**
 * Use this function to format the byte size.
 *
 * @exmaple
 * ```js
 * import { formatSizeUnits } from 'bucky.js';
 *
 * console.log(formatSizeUnits(15393763));
 * ```
 */
declare function formatSizeUnits(bytes: number): string;
export { formatSizeUnits };
