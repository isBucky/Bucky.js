/**
 * A function that generates random hexadecimal.
 *
 * @example
 * ```js
 * import { generateHex } from 'bucky.js';
 *
 * console.log(generateHex());
 * ```
 */
declare function generateHex(): string;
export { generateHex };
