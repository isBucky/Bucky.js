/**
 * Use this function to check if a file is empty.
 *
 * @example
 * ```js
 * import { isEmptyFile } from 'bucky.js';
 *
 * console.log(isEmptyFile('/path/to/file'));
 * ```
*/
declare function isEmptyFile(file: string): boolean;
export { isEmptyFile };
