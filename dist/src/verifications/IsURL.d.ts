/**
 * Use this function to check if it's a URL.
 *
 * @example
 * ```js
 * import { isURL } from 'bucky.js';
 *
 * console.log(isURL('https://www.npmjs.com/package/bucky.js'));
 * ```
 */
declare function isURL(link: string): boolean;
export { isURL };
